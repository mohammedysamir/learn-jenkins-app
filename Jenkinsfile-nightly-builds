pipeline {
    agent any
    stages {
        stage('Build Docker Images') {
            agent any
            steps {
                sh 'docker build -f ci/dockerfile-playwright -t my-playwright-app .'
                sh 'docker build -f ci/dockerfile-awsCli -t my-aws-cli-app .'
            }
        }
    }
}
